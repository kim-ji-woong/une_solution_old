#define MyAppVersion "1.1.9"
; Script generated by the Inno Setup Script Wizard.
; SEE THE DOCUMENTATION FOR DETAILS ON CREATING INNO SETUP SCRIPT FILES!



[Setup] 
; NOTE: The value of AppId uniquely identifies this application.
; Do not use the same AppId value in installers for other applications.
; (To generate a new GUID, click Tools | Generate GUID inside the IDE.)
AppId={{A7D3F375-C145-423E-88F6-DEE9A6992317}
AppName=KpxMonitoring
AppVersion=1.0
;AppVerName=KpxMonitoring 1.0
AppPublisher=UNE
AppPublisherURL=http://www.unes.co.kr/
AppSupportURL=http://www.unes.co.kr/
AppUpdatesURL=http://www.unes.co.kr/
DefaultDirName=C:\UNE\KpxMonitoring
DisableProgramGroupPage=yes
UsePreviousAppDir=no
OutputDir=D:\Installer\output
OutputBaseFilename=KPXMonitoring-{#MyAppVersion}_setup
SetupIconFile=D:\Installer\Data\KPX\kpx.ico
Compression=lzma
SolidCompression=yes
PrivilegesRequired=poweruser

[Languages]
Name: "english"; MessagesFile: "compiler:Default.isl"
Name: "korean"; MessagesFile: "compiler:Languages\Korean.isl"

[Tasks]
Name: "desktopicon"; Description: "{cm:CreateDesktopIcon}"; GroupDescription: "{cm:AdditionalIcons}"; Flags: unchecked

[Files]
Source: "D:\Installer\Data\KPX\AlarmSound.WAV"; DestDir: "{app}"; Flags: ignoreversion
Source: "D:\Installer\Data\KPX\config.ini"; DestDir: "{app}"; Flags: ignoreversion
Source: "D:\Installer\Data\KPX\DBUtil.dll"; DestDir: "{app}"; Flags: ignoreversion
Source: "D:\Installer\Data\KPX\Geometrydn.dll"; DestDir: "{app}"; Flags: ignoreversion
Source: "D:\Installer\Data\KPX\HelpViewer.exe"; DestDir: "{app}\Help";Flags: ignoreversion
Source: "D:\Installer\Data\KPX\kpx.ico"; DestDir: "{app}"; Flags: ignoreversion
Source: "D:\Installer\Data\KPX\KpxMonitoring.exe"; DestDir: "{app}"; Flags: ignoreversion overwritereadonly
Source: "D:\Installer\Data\KPX\KpxMonitoring.exe.config"; DestDir: "{app}"; Flags: ignoreversion
Source: "D:\Installer\Data\KPX\libData.dll"; DestDir: "{app}"; Flags: ignoreversion
Source: "D:\Installer\Data\KPX\NanumBarunGothic.ttf"; DestDir: "{fonts}"; FontInstall: "³ª´®¹Ù¸¥°íµñ"; Flags: onlyifdoesntexist uninsneveruninstall
Source: "D:\Installer\Data\KPX\PipeHistoryLocalService.exe"; DestDir: "{app}"; Flags: ignoreversion
Source: "D:\Installer\Data\KPX\ReadFileManager.dll"; DestDir: "{app}"; Flags: ignoreversion
Source: "D:\Installer\Data\KPX\SystemInfo.ini"; DestDir: "{app}"; Flags: ignoreversion
Source: "D:\Installer\Data\KPX\UEControl.dll"; DestDir: "{app}"; Flags: ignoreversion 
Source: "D:\Installer\Data\KPX\vcredist_x86.exe"; DestDir: "{app}"; Flags: ignoreversion
; NOTE: Don't use "Flags: ignoreversion" on any shared system files

[Code]

// function IsServiceInstalled(ServiceName: string) : boolean;
// function IsServiceRunning(ServiceName: string) : boolean;
// function InstallService(FileName, ServiceName, DisplayName, Description : string;ServiceType,StartType :cardinal) : boolean;
// function RemoveService(ServiceName: string) : boolean;
// function StartService(ServiceName: string) : boolean;
// function StopService(ServiceName: string) : boolean;
// function SetupService(service, port, comment: string) : boolean;

type
    SERVICE_STATUS = record
        dwServiceType               : cardinal;
        dwCurrentState              : cardinal;
        dwControlsAccepted          : cardinal;
        dwWin32ExitCode             : cardinal;
        dwServiceSpecificExitCode   : cardinal;
        dwCheckPoint                : cardinal;
        dwWaitHint                  : cardinal;
    end;
    HANDLE = cardinal;

const
    SERVICE_QUERY_CONFIG        = $1;
    SERVICE_CHANGE_CONFIG       = $2;
    SERVICE_QUERY_STATUS        = $4;
    SERVICE_START               = $10;
    SERVICE_STOP                = $20;
    SERVICE_ALL_ACCESS          = $f01ff;
    SC_MANAGER_ALL_ACCESS       = $f003f;
    SERVICE_WIN32_OWN_PROCESS   = $10;
    SERVICE_WIN32_SHARE_PROCESS = $20;
    SERVICE_WIN32               = $30;
    SERVICE_INTERACTIVE_PROCESS = $100;
    SERVICE_BOOT_START          = $0;
    SERVICE_SYSTEM_START        = $1;
    SERVICE_AUTO_START          = $2;
    SERVICE_DEMAND_START        = $3;
    SERVICE_DISABLED            = $4;
    SERVICE_DELETE              = $10000;
    SERVICE_CONTROL_STOP        = $1;
    SERVICE_CONTROL_PAUSE       = $2;
    SERVICE_CONTROL_CONTINUE    = $3;
    SERVICE_CONTROL_INTERROGATE = $4;
    SERVICE_STOPPED             = $1;
    SERVICE_START_PENDING       = $2;
    SERVICE_STOP_PENDING        = $3;
    SERVICE_RUNNING             = $4;
    SERVICE_CONTINUE_PENDING    = $5;
    SERVICE_PAUSE_PENDING       = $6;
    SERVICE_PAUSED              = $7;

// #######################################################################################
// nt based service utilities
// #######################################################################################
function OpenSCManager(lpMachineName, lpDatabaseName: string; dwDesiredAccess :cardinal): HANDLE;
external 'OpenSCManagerW@advapi32.dll stdcall';

function OpenService(hSCManager :HANDLE;lpServiceName: string; dwDesiredAccess :cardinal): HANDLE;
external 'OpenServiceW@advapi32.dll stdcall';

function CloseServiceHandle(hSCObject :HANDLE): boolean;
external 'CloseServiceHandle@advapi32.dll stdcall';

function CreateService(hSCManager :HANDLE;lpServiceName, lpDisplayName: string;dwDesiredAccess,dwServiceType,dwStartType,dwErrorControl: cardinal;lpBinaryPathName,lpLoadOrderGroup: String; lpdwTagId : cardinal;lpDependencies,lpServiceStartName,lpPassword :string): cardinal;
external 'CreateServiceW@advapi32.dll stdcall';

function DeleteService(hService :HANDLE): boolean;
external 'DeleteService@advapi32.dll stdcall';

function StartNTService(hService :HANDLE;dwNumServiceArgs : cardinal;lpServiceArgVectors : cardinal) : boolean;
external 'StartServiceW@advapi32.dll stdcall';

function ControlService(hService :HANDLE; dwControl :cardinal;var ServiceStatus :SERVICE_STATUS) : boolean;
external 'ControlService@advapi32.dll stdcall';

function QueryServiceStatus(hService :HANDLE;var ServiceStatus :SERVICE_STATUS) : boolean;
external 'QueryServiceStatus@advapi32.dll stdcall';

function QueryServiceStatusEx(hService :HANDLE;ServiceStatus :SERVICE_STATUS) : boolean;
external 'QueryServiceStatus@advapi32.dll stdcall';

function GetLastError() : cardinal;
external 'GetLastError@kernel32.dll stdcall';

function OpenServiceManager() : HANDLE;
begin
    if UsingWinNT() = true then begin
        Result := OpenSCManager('','',SC_MANAGER_ALL_ACCESS);
        if Result = 0 then
            MsgBox('the servicemanager is not available', mbError, MB_OK)
    end
    else begin
            MsgBox('only nt based systems support services', mbError, MB_OK)
            Result := 0;
    end
end;

function IsServiceInstalled(ServiceName: string) : boolean;
var
    hSCM    : HANDLE;
    hService: HANDLE;
begin
    hSCM := OpenServiceManager();
    Result := false;
    if hSCM <> 0 then begin
        hService := OpenService(hSCM,ServiceName,SERVICE_QUERY_CONFIG);
        if hService <> 0 then begin
            Result := true;
            CloseServiceHandle(hService)
        end;
        CloseServiceHandle(hSCM)
    end
end;

function InstallService(FileName, ServiceName, DisplayName, Description : string;ServiceType,StartType :cardinal) : boolean;
var
    hSCM    : HANDLE;
    hService: HANDLE;
begin
    hSCM := OpenServiceManager();
    Result := false;
    if hSCM <> 0 then begin
        hService := CreateService(hSCM,ServiceName,DisplayName,SERVICE_ALL_ACCESS,ServiceType,StartType,0,FileName,'',0,'','','');
        if hService <> 0 then begin
            Result := true;
            // Win2K & WinXP supports aditional description text for services
            if Description<> '' then
                RegWriteStringValue(HKLM,'System\CurrentControlSet\Services\' + ServiceName,'Description',Description);
            CloseServiceHandle(hService)
        end;
        CloseServiceHandle(hSCM)
    end
end;

function RemoveService(ServiceName: string) : boolean;
var
    hSCM    : HANDLE;
    hService: HANDLE;
begin
    hSCM := OpenServiceManager();
    Result := false;
    if hSCM <> 0 then begin
        hService := OpenService(hSCM,ServiceName,SERVICE_DELETE);
        if hService <> 0 then begin
            Result := DeleteService(hService);
            CloseServiceHandle(hService)
        end;
        CloseServiceHandle(hSCM)
    end
end;

function StartService(ServiceName: string) : boolean;
var
    hSCM    : HANDLE;
    hService: HANDLE;
begin
    hSCM := OpenServiceManager();
    Result := false;
    if hSCM <> 0 then begin
        hService := OpenService(hSCM,ServiceName,SERVICE_START);
        if hService <> 0 then begin
            Result := StartNTService(hService,0,0);
            CloseServiceHandle(hService)
        end;
        CloseServiceHandle(hSCM)
    end;
end;

function StopService(ServiceName: string) : boolean;
var
    hSCM    : HANDLE;
    hService: HANDLE;
    Status  : SERVICE_STATUS;
begin
    hSCM := OpenServiceManager();
    Result := false;
    if hSCM <> 0 then begin
        hService := OpenService(hSCM,ServiceName,SERVICE_STOP);
        if hService <> 0 then begin
            Result := ControlService(hService,SERVICE_CONTROL_STOP,Status);
            CloseServiceHandle(hService)
        end;
        CloseServiceHandle(hSCM)
    end;
end;

function IsServiceRunning(ServiceName: string) : boolean;
var
    hSCM    : HANDLE;
    hService: HANDLE;
    Status  : SERVICE_STATUS;
begin
    hSCM := OpenServiceManager();
    Result := false;
    if hSCM <> 0 then begin
        hService := OpenService(hSCM,ServiceName,SERVICE_QUERY_STATUS);
        if hService <> 0 then begin
            if QueryServiceStatus(hService,Status) then begin
                Result :=(Status.dwCurrentState = SERVICE_RUNNING)
            end;
            CloseServiceHandle(hService)
            end;
        CloseServiceHandle(hSCM)
    end
end;

function IsServiceStopped(ServiceName: string) : boolean;
var
    hSCM    : HANDLE;
    hService: HANDLE;
    Status  : SERVICE_STATUS;
begin
    hSCM := OpenServiceManager();
    Result := false;
    if hSCM <> 0 then begin
        hService := OpenService(hSCM,ServiceName,SERVICE_QUERY_STATUS);
        if hService <> 0 then begin
            if QueryServiceStatus(hService,Status) then begin
                Result :=(Status.dwCurrentState = SERVICE_STOPPED)
            end;
            CloseServiceHandle(hService)
            end;
        CloseServiceHandle(hSCM)
    end
end;

function InitializeSetup(): boolean;
var
  ResultCode: integer;
  begin

    // Launch Notepad and wait for it to terminate
    if RegKeyExists(HKLM,'SYSTEM\CurrentControlSet\Services\PipeHistoryLocalService') then
        begin
          // handle success if necessary; ResultCode contains the exit code
          //Exec(ExpandConstant('{sys}\sc.exe'), 'stop PipeHistoryLocalService', '', SW_HIDE, ewNoWait, ResultCode)
          //Exec(ExpandConstant('{sys}\sc.exe'), 'delete PipeHistoryLocalService', '', SW_HIDE, ewNoWait, ResultCode)
          if (IsServiceStopped( 'PipeHistoryLocalService' ) <> True) then
          begin
            begin				
              StopService('PipeHistoryLocalService')
            end;
            while (IsServiceStopped( 'PipeHistoryLocalService' ) <> False) do
            begin
               StopService('PipeHistoryLocalService')			   
            end;
          end;
        end
    else begin
      // handle failure if necessary; ResultCode contains the error code
    end;

  // Proceed Setup
  Result := True;

end;

function IsAppRunning(const FileName : string): Boolean;
begin                                                                
  Result := (FindWindowByClassName( FileName) <> 0) or (FindWindowByClassName( FileName) <> 0);
end;

function InitializeUninstall(): Boolean;
var
  ResultCode: integer;
  begin
      if (IsServiceStopped( 'PipeHistoryLocalService' ) <> True) then
      begin
        begin				
          StopService('PipeHistoryLocalService')
        end;

        while (IsServiceStopped( 'PipeHistoryLocalService' ) <> False) do
        begin
           StopService('PipeHistoryLocalService')			   
        end;
      end;  

      begin
         RemoveService( 'PipeHistoryLocalService' ) 
      end;
      
  Result := True;

end; 

[Icons]
Name: "{commonprograms}\KpxMonitoring"; Filename: "{app}\KpxMonitoring.exe"
Name: "{commondesktop}\KpxMonitoring"; Filename: "{app}\KpxMonitoring.exe"; Tasks: desktopicon

[Registry] 
Root: HKCU; Subkey: "Software\UNE\Site\500\Server Connection Info"; ValueType: string; ValueName: "db_name"; ValueData: "KPX"
Root: HKCU; Subkey: "Software\UNE\Site\500\Server Connection Info"; ValueType: string; ValueName: "db_type"; ValueData: "1"
Root: HKCU; Subkey: "Software\UNE\Site\500\Server Connection Info"; ValueType: string; ValueName: "dxf_ftp_url"; ValueData: ""
Root: HKCU; Subkey: "Software\UNE\Site\500\Server Connection Info"; ValueType: string; ValueName: "page_encoding"; ValueData: "65001"
Root: HKCU; Subkey: "Software\UNE\Site\500\Server Connection Info"; ValueType: string; ValueName: "time_server"; ValueData: "time2.kriss.re.kr"
Root: HKCU; Subkey: "Software\UNE\Site\500\Server Connection Info"; ValueType: string; ValueName: "webserver_url"; ValueData: "http://211.34.0.91:8080/SOP"
 
[Run]
Filename: "{app}\vcredist_x86.exe"; Description: "{cm:LaunchProgram,vcredist_x86}"; Flags: runhidden 
Filename: "{sys}\sc.exe"; Parameters: "create PipeHistoryLocalService binpath= ""{app}\PipeHistoryLocalService.exe"" start= auto" ; Flags: runhidden 
Filename: "{sys}\sc.exe"; Parameters: "start PipeHistoryLocalService" ; Flags: runhidden 
Filename: "{app}\KpxMonitoring.exe"; Description: "{cm:LaunchProgram,KpxMonitoring}"; Flags: nowait postinstall skipifsilent

[Setup]         
SetupLogging=yes
        
[UninstallRun] 
Filename: "{sys}\sc.exe"; Parameters: "stop PipeHistoryLocalService" ; Flags: runhidden waituntilterminated
Filename: "{sys}\sc.exe"; Parameters: "delete PipeHistoryLocalService" ; Flags: runhidden waituntilterminated
;Filename: "{sys}\cmd.exe"; Parameters: "/C rmdir /S /Q C:\ProgramData\UnE\KPX" ; Flags: runhidden waituntilterminated
;Filename: "{sys}\cmd.exe"; Parameters: "/C rmdir /S /Q {app}" ; Flags: runhidden waituntilterminated

[UninstallDelete]
Name: "{commonappdata}\UnE\*.*"; Type: filesandordirs
Name: "{app}\*.*"; Type: filesandordirs
 
 