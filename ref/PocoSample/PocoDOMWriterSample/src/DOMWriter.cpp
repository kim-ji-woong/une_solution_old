//
// DOMWriter.cpp
//
// $Id: //poco/1.4/XML/samples/DOMWriter/src/DOMWriter.cpp#1 $
//
// This sample demonstrates the DOMWriter class and how to
// build DOM documents in memory.
//
// Copyright (c) 2004-2006, Applied Informatics Software Engineering GmbH.
// and Contributors.
//
// Permission is hereby granted, free of charge, to any person or organization
// obtaining a copy of the software and accompanying documentation covered by
// this license (the "Software") to use, reproduce, display, distribute,
// execute, and transmit the Software, and to prepare derivative works of the
// Software, and to permit third-parties to whom the Software is furnished to
// do so, all subject to the following:
// 
// The copyright notices in the Software and this entire statement, including
// the above license grant, this restriction and the following disclaimer,
// must be included in all copies of the Software, in whole or in part, and
// all derivative works of the Software, unless such copies or derivative
// works are solely in the form of machine-executable object code generated by
// a source language processor.
// 
// THE SOFTWARE IS PROVIDED "AS IS", WITHOUT WARRANTY OF ANY KIND, EXPRESS OR
// IMPLIED, INCLUDING BUT NOT LIMITED TO THE WARRANTIES OF MERCHANTABILITY,
// FITNESS FOR A PARTICULAR PURPOSE, TITLE AND NON-INFRINGEMENT. IN NO EVENT
// SHALL THE COPYRIGHT HOLDERS OR ANYONE DISTRIBUTING THE SOFTWARE BE LIABLE
// FOR ANY DAMAGES OR OTHER LIABILITY, WHETHER IN CONTRACT, TORT OR OTHERWISE,
// ARISING FROM, OUT OF OR IN CONNECTION WITH THE SOFTWARE OR THE USE OR OTHER
// DEALINGS IN THE SOFTWARE.
//


#include "Poco/DOM/Document.h"
#include "Poco/DOM/Element.h"
#include "Poco/DOM/Text.h"
#include "Poco/DOM/Attr.h"
#include "Poco/DOM/AttrMap.h"
#include "Poco/DOM/AutoPtr.h"
#include "Poco/DOM/DOMWriter.h"
#include "Poco/XML/XMLWriter.h"
#include "Poco/DOM/Comment.h"
#include <iostream>
#include <fstream>
#include "Poco/TextConverter.h"
#include "Poco/ASCIIEncoding.h"
#include "Poco/Latin1Encoding.h"
#include "Poco/Latin2Encoding.h"
#include "Poco/Latin9Encoding.h"
#include "Poco/UTF8Encoding.h"
#include "Poco/Windows1250Encoding.h"
#include "Poco/Windows1252Encoding.h"
#include "Poco/Windows1251Encoding.h"

using Poco::XML::Attr;
using Poco::XML::AttrMap;
using Poco::XML::Document;
using Poco::XML::Element;
using Poco::XML::Text;
using Poco::XML::AutoPtr;
using Poco::XML::DOMWriter;
using Poco::XML::XMLWriter;
using Poco::XML::Comment;

using Poco::TextConverter;
using Poco::ASCIIEncoding;
using Poco::Latin1Encoding;
using Poco::Latin2Encoding;
using Poco::Latin9Encoding;
using Poco::UTF8Encoding;
using Poco::Windows1250Encoding;
using Poco::Windows1251Encoding;
using Poco::Windows1252Encoding;

#include <windows.h>

int UnicodeToMbcs(char* pszDst, const wchar_t* pwzIn, unsigned int uCodepage)
{
	int nReqLen = (int)WideCharToMultiByte(uCodepage, 0, pwzIn, (int)wcslen(pwzIn), NULL, 0, NULL, NULL);
	int nLen    = (int)WideCharToMultiByte(uCodepage, 0, pwzIn, (int)wcslen(pwzIn), pszDst, nReqLen, NULL, NULL); 
	if(nLen)
		pszDst[nLen] = 0;
	return nLen;
} 

int MbcsToUnicode(wchar_t* pwzDst, const char* pszIn, unsigned int uCodepage)
{ 
	int nReqLen = (int)MultiByteToWideChar(uCodepage, 0, pszIn, (int)strlen(pszIn), 0, 0 ); 
	int nLen    = (int)MultiByteToWideChar(uCodepage, 0, pszIn, (int)strlen(pszIn), pwzDst, nReqLen );
	if(nLen)
		pwzDst[nLen] = 0;
	return nLen; 
}

int UTF8toAscii(char* pszAcp,  const char* pszUTF8)
{
	wchar_t wzStr[MAX_PATH];
	if(strlen(pszUTF8)>0) {
		MbcsToUnicode(wzStr, pszUTF8, CP_UTF8);	
		return UnicodeToMbcs(pszAcp, wzStr, CP_ACP);
	}

	pszAcp[0] = '\0';
	return 0;
}

int AsciiToUTF8(char* pszUTF8, const char* pszAcp)
{
	wchar_t wzStr[MAX_PATH];
	if(strlen(pszAcp)>0) {
		MbcsToUnicode(wzStr, pszAcp, CP_ACP);	
		return UnicodeToMbcs(pszUTF8, wzStr, CP_UTF8);
	}
	pszUTF8[0] = '\0';
	return 0;
}


int main(int argc, char** argv)
{
	// build a DOM document and write it to standard output.

	AutoPtr<Document> pDoc = new Document;
	
	AutoPtr<Element> pRoot = pDoc->createElement("UnE_SDK");
	pRoot->setAttribute("ID", "1");

	Comment * pComment3 =pDoc->createComment("this is root");
	pDoc->appendChild(pComment3);
	pDoc->appendChild(pRoot);

	//Attr(Document* pOwnerDocument, Element* pOwnerElement, const XMLString& namespaceURI, const XMLString& localName, const XMLString& qname, const XMLString& value, bool specified = true);

	AutoPtr<Element> pChild1 = pDoc->createElement("child1");
	AutoPtr<Text> pText1 = pDoc->createTextNode("text1");
	pChild1->appendChild(pText1);
	
	char buf[512];
	
	Comment * pComment =pDoc->createComment("this is element1");

	AsciiToUTF8(buf,"한글 comment");
	Comment * pComment2 =pDoc->createComment(std::string(buf));
	pRoot->appendChild(pComment);
	pRoot->appendChild(pChild1);
	pRoot->appendChild(pComment2);

	std::string latin1String("한글");
	std::string utf8String;
	ASCIIEncoding latin1;

	Latin2Encoding latin2;
	Latin9Encoding latin9;
	UTF8Encoding utf8;
	TextConverter converter(latin1, utf8);
	converter.convert(latin1String, utf8String);

	
	AsciiToUTF8(buf, latin1String.c_str());
	AutoPtr<Element> pChild2 = pDoc->createElement(std::string(buf));
	AutoPtr<Text> pText2 = pDoc->createTextNode(std::string(buf));
	pChild2->appendChild(pText2);
	pRoot->appendChild(pChild2);
	
	DOMWriter writer;
	writer.setNewLine("\n");
	writer.setOptions(XMLWriter::PRETTY_PRINT);
	std::ofstream str("test.xml");
	writer.writeNode(str, pDoc);
	
	return 0;
}
