// stdafx.cpp : 표준 포함 파일만 들어 있는 소스 파일입니다.
// UBML.pch는 미리 컴파일된 헤더가 됩니다.
// stdafx.obj에는 미리 컴파일된 형식 정보가 포함됩니다.

#include "stdafx.h"

// TODO: 필요한 추가 헤더는
// 이 파일이 아닌 STDAFX.H에서 참조합니다.

const size_t SIZE_BYTE		= sizeof(unsigned char);
const size_t SIZE_CHAR		= sizeof(char);
const size_t SIZE_WCHAR		= sizeof(wchar_t);
const size_t SIZE_SHORT		= sizeof(short);
const size_t SIZE_LONG		= sizeof(int);
const size_t SIZE_LONGLONG	= sizeof(__int64);
const size_t SIZE_FLOAT		= sizeof(float);
const size_t SIZE_DOUBLE	= sizeof(double);
